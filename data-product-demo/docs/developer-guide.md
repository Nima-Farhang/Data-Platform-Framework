# Developer Guide

## What To Look At

### Shared platform usage
- `refined_customers.sql`
- `refined_orders.sql`

### Governance example
- `apply_masking_policy_tags()` use in `refined_customers.sql`

### Incremental example
- `get_max_timestamp()` use in `refined_orders.sql`

### Audit metadata example
- `add_audit_columns()` use in refined models

## Suggested Exercises
- add one more refined model
- extend masking rules
- improve query tag format
- add schema.yml files
- add source definitions
